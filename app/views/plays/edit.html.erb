<h2>Play Session - <%= @play.logged_time %> with <%= @play.owner.name %></h2>
<%= form_for @play do |f| %>
<%= f.label :notes %>
<%= f.text_area :notes, value: "#{@play.notes}" %><br>
<%= f.submit %>
<% end %>
